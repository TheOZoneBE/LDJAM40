export default class ZoneRenderer {
    constructor(group){
        this.group = group;
        this.zones = new Map();
        this.xOffset = xOffset;
        this.yOffset = yOffset;
    }

    updateGraphics(){

    }

    setOffset(xOffset, yOffset){
        this.xOffset = xOffset;
        this.yOffset = yOffset;

        //update sprites
    }

    addZone(zone){

    }

    removeZone(zone){

    }
}